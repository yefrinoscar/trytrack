# D1 migrations

Wrangler applies these with `wrangler d1 migrations apply`.

- `0000_init.sql` is generated by drizzle-kit; regenerate with `vp run db:generate`
  and copy the new file here.
- Applied automatically on deploy via `migrations_dir` in the wrangler config.
