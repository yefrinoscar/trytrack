node_modules
.DS_Store
dist
dist-ssr
*.local
.env
.env.local
.nitro
.tanstack
.wrangler
.output
.vinxi
__unconfig*
todos.json
.npm-cache

# Local D1/SQLite files (Cloudflare uses the `DB` binding instead).
local.db
local.db-*
*.sqlite
*.sqlite-*

# Cloudflare wrangler local state
.wrangler/state

secrets
